// 导入Vue和Vuex
import Vue from "Vue";
import Vuex from "vuex";

// 使用Vuex插件
Vue.use(Vuex);

// 导出一个Vuex.Store实例
export default new Vuex.Store({
  // 定义状态
  state:{},
  // 定义同步的mutation
  mutation:{},
  // 定义异步的action
  actions:{},
  // 定义模块
  modules:{}
});