---
view-count: 6
update: 2026-01-08 14:12
related:
  - "[[2025-12-20-常用开发标记]]"
  - "[[commit 和 PR的选择]]"
  - "[[200组开发选题]]"
  - "[[如何为开源项目贡献你的第一个PR]]"
---

## 🎯 Conventional Commits & Responses API：认知锚点库

---

## **Part1：Conventional Commits（约定式提交 ）**

### **第一层：基石概念** ★

| 概念                       | 压缩核心                                                         | 为什么                                  |
| ------------------------ | ------------------------------------------------------------ | ------------------------------------ |
| **Conventional Commits** | Git提交消息格式规范                                                  | 清晰历史 + 自动化版本管理 + 生成Changelog         |
| **基本格式**                 | `<type>[scope]: <description>`                               | 机器&人类可读                              |
| **Type（类型）**             | feat/fix/docs/style/refactor/perf/test/build/ci/chore/revert | 分类：特性、修复、文档等                         |
| **Scope（作用域）**           | 可选，指定影响的模块                                                   | 如 `feat(auth): ...` 标识认证模块           |
| **Description（描述）**      | <50字，祈使句，现在时                                                 | 简洁清晰                                 |
| **Body（正文）**             | 可选，解释"为什么"而非"怎么做"                                            | 补充上下文                                |
| **Footer（脚注）**           | 可选，关闭issue或标记破坏性变更                                           | 如 `Closes #123` / `BREAKING CHANGE:` |

### **第二层：提交类型速查表** △

| 类型           | 含义      | 示例                                | 版本号影响         |
| ------------ | ------- | --------------------------------- | ------------- |
| **feat**     | 新功能     | `feat(user): add social login`    | 次版本↑ (1.x.0)  |
| **fix**      | 修复Bug   | `fix(api): handle empty requests` | 补丁版本↑ (1.0.x) |
| **docs**     | 仅文档     | `docs: update README`             | 无版本变化         |
| **style**    | 格式（非逻辑） | `style: format code`              | 无版本变化         |
| **refactor** | 代码重构    | `refactor: simplify algorithm`    | 无版本变化         |
| **perf**     | 性能优化    | `perf(search): optimize query`    | 补丁版本↑         |
| **test**     | 测试代码    | `test(auth): add unit tests`      | 无版本变化         |
| **build**    | 构建系统    | `build: update npm deps`          | 无版本变化         |
| **ci**       | CI配置    | `ci: add Node.js version`         | 无版本变化         |
| **chore**    | 其他杂务    | `chore: update scripts`           | 无版本变化         |
| **revert**   | 回滚提交    | `revert: commit abc123`           | 补丁版本↑         |

### **第三层：提交消息规范清单**

✅ **提交前**：

- 检查暂存内容：`git --no-pager diff --staged --stat`
- 审查详细变更：`git --no-pager diff --staged`

✅ **写消息**：

- ✓ 使用 Conventional Commits 格式
- ✓ 主题 ≤ 72字符
- ✓ 动词用祈使句、现在时（"add"、"fix"，非"added"、"fixed"）
- ✓ 避免占位符（"WIP""commit local work"）
- ✓ 若有正文，用空行分隔主题

✅ **合并提交**：

- 跳过自定义前缀
- 用清晰标题：`Merge origin/main: <what changed and how conflicts resolved>`

### **第四层：实战示例**

```markdown
## 好的提交 ✓

feat(tui/history): show exit code and duration for Exec cells
- 格式正确、作用域清晰、描述简洁

fix(core/exec): handle SIGINT in on_exec_command_begin
- 避免孤立子进程

docs(agents): clarify commit-message expectations
- 文档类型、内容清晰

## 坏的提交 ✗

chore: commit local work
- 占位符，无含义

fixed a bug
- 无Type前缀

update everything
- 描述不清、无作用域
```

### **第五层：自动化价值** ★

| 工具                      | 功能           | 例子                             |
| ----------------------- | ------------ | ------------------------------ |
| **Commitlint**          | 校验提交消息格式     | 非法格式自动拒绝                       |
| **Standard Version**    | 自动生成版本号（语义化） | `feat` → v1.1.0，`fix` → v1.0.1 |
| **Changelog Generator** | 自动生成变更日志     | 按type分组：Features/Fixes/Docs    |

**启用方式**：

```bash
# 安装提交验证
npm install commitlint @commitlint/config-conventional husky

# 自动版本管理
npm install standard-version

# 生成Changelog
standard-version --release-as minor
```

### **第六层：反常识** ❗

| 陷阱                    | 真相                          |
| --------------------- | --------------------------- |
| ❗ Type太多选择，随便选？       | ✗ Type决定版本号；乱选导致版本号错误       |
| ❗ Scope随便写？           | ✗ Scope标准化 → 可自动化分析、统计      |
| ❗ 详细正文越长越好？           | ✗ 正文应解释"为什么"而非"做了什么"（代码已说明） |
| ❗ BREAKING CHANGE非必须？ | ✗ 破坏性变更必须标记 → 触发major版本号    |

---

## **快速参考**

### **Conventional Commits 速记**

```markdown
格式：type(scope): description

# 最常用的3种
feat(module): add feature          → 次版本↑
fix(module): correct bug           → 补丁版本↑
docs: update docs                  → 无版本变化

# 破坏性变更（必须标记）
feat(api)!: redesign endpoints
或
feat(api): redesign endpoints
BREAKING CHANGE: old API deprecated
```

---

## **关键对标**

### **Conventional Commits vs 传统方式**

| 维度              | 传统提交 | Conventional Commits |
| --------------- | ---- | -------------------- |
| **可读性**         | ⭐⭐   | ⭐⭐⭐⭐⭐                |
| **自动化程度**       | ⭐    | ⭐⭐⭐⭐⭐                |
| **版本管理**        | 手动   | 自动                   |
| **Changelog生成** | 不可能  | 自动生成                 |
| **团队协作**        | 低效   | 高效                   |

---

## **复习触发点**

| 时长    | 内容                              | 用途     |
| ----- | ------------------------------- | ------ |
| 3min  | Type速查表 + 基本格式                  | 快速参考   |
| 10min | 规范清单 + 自动化工具                    | 理解完整流程 |
| 15min | API架构 + 参数配置                    | 开发集成   |
| 提交时   | *"feat还是fix？"* → 查Type表         | 正确分类   |
| 开发时   | *"该用model_reasoning_summary吗？"* | 场景判断   |

---

**生成时间**：2025-12-14 | **版本**：1.0 | **用途**：工程规范/API集成参考
