## 🔗 相关链接

- **主索引**: [[Atlas/Index/仓库标签管理系统 | 仓库标签管理系统]]
- **相关索引**: [[Atlas/MOCs.md | 系统主索引]]
- **关联报告**: 标签规范化系列报告

---

# 标签映射表

**更新时间**: 2026-01-25
**版本**: v1.0
**状态**: 🟡 Phase 1 完成，待执行

---

## 📋 使用说明

本文档定义了所有旧标签到新标签的映射关系，用于标签规范化改造。

**执行顺序**：
1. 先删除无效/占位符标签
2. 再替换旧标签为新标签
3. 最后统一标签格式

---

## 🗑️ 第一阶段：删除无效/占位符标签

### 模板占位符（需删除）

| 标签 | 出现次数 | 说明 | 示例文件 |
|------|----------|------|----------|
| `#Domain/<SubDomain>` | 15 | 模板占位符 | 多个模板文件 |
| `#Status/<State>` | 11 | 模板占位符 | 多个模板文件 |
| `#Type/<ContentType>` | 11 | 模板占位符 | 多个模板文件 |

### 不完整标签（需删除）

| 标签 | 出现次数 | 说明 | 示例文件 |
|------|----------|------|----------|
| `#Domain/` | 13 | 不完整 | 待统计 |
| `#Type/` | 8 | 不完整 | 待统计 |
| `#Status/` | 7 | 不完整 | 待统计 |

### 无效标签（需删除）

| 标签 | 出现次数 | 说明 | 处理方式 |
|------|----------|------|----------|
| `#6f9` | 8 | 无效标签 | 删除 |
| `#333` | 8 | 无效标签 | 删除 |
| `#SpecDriven` | 4 | 非标签格式 | 删除 |
| `#javascriptnodejs-testing-best-practices-guide-advanced-testing-techniques` | 4 | 文件名误用 | 删除 |

---

## 🔄 第二阶段：标签标准化替换

### Domain 标签映射

#### 高频标签（优先处理）

| 旧标签 | 出现次数 | 新标签 | 优先级 | 说明 |
|--------|----------|--------|--------|------|
| `#AI` | 30 | `#Domain/AI` | 🔴 高 | 已有标准标签 |
| `#Domain/Cognitive` | 28 | `#Domain/Cognitive` | ✅ 无需修改 | 已符合规范 |
| `#Domain/AI` | 46 | `#Domain/AI` | ✅ 无需修改 | 已符合规范 |

#### 中频标签

| 旧标签 | 出现次数 | 新标签 | 优先级 | 说明 |
|--------|----------|--------|--------|------|
| `#Domain/Cognition` | 6 | `#Domain/Cognitive` | 🟡 中 | 拼写统一 |
| `#Domain/CognitiveSystem` | 6 | `#Domain/Cognitive` | 🟡 中 | 简化层级 |
| `#Domain/ContentCreation` | 6 | `#Domain/Content` | 🟡 中 | 简化层级 |
| `#Domain/Productivity` | 6 | `#Domain/Productivity` | ✅ 保留 | 已符合规范 |
| `#Domain/TechStack` | 7 | `#Domain/TechStack` | ✅ 保留 | 已符合规范 |
| `#SubDomain/IELTS` | 6 | `#Domain/Language/IELTS` | 🟡 中 | 已有标准标签 |

#### 低频标签

| 旧标签 | 出现次数 | 新标签 | 优先级 | 说明 |
|--------|----------|--------|--------|------|
| `#Python` | 9 | `#Domain/Technology/Python` | 🟢 低 | 技术栈子分类 |
| `#Architecture` | 8 | `#Domain/TechStack/SystemDesign` | 🟢 低 | 系统架构 |
| `#SystemDesign` | 8 | `#Domain/TechStack/SystemDesign` | 🟢 低 | 系统设计 |
| `#Tech/AI` | 5 | `#Domain/AI` | 🟢 低 | 旧格式 |
| `#OpenSource` | 4 | `#Domain/Technology/OpenSource` | 🟢 低 | 转换为 Domain |
| `#ContentCreation` | 5 | `#Domain/Content` | 🟢 低 | 内容创建 |
| `#card` | 4 | `#Type/Card` | 🟢 低 | 转换为 Type |
| `#Domain/Psychology` | 5 | `#Domain/Cognitive/Psychology` | 🟢 低 | 归类到认知 |
| `#Domain/MentalModel` | 4 | `#Domain/Cognitive/MentalModel` | 🟢 低 | 归类到认知 |
| `#Concurrency` | 4 | `#Domain/TechStack/Concurrency` | 🟢 低 | 技术主题 |
| `#CloudNative` | 4 | `#Domain/TechStack/CloudNative` | 🟢 低 | 技术主题 |
| `#CareerPlanning` | 4 | `#Domain/Career/Planning` | 🟢 低 | 职业规划 |
| `#EngineeringMindset` | 4 | `#Domain/Career/Engineering` | 🟢 低 | 工程思维 |

### Status 标签映射

| 旧标签 | 出现次数 | 新标签 | 优先级 | 说明 |
|--------|----------|--------|--------|------|
| `#todo` | 8 | `#Status/TODO` | 🔴 高 | 已有标准标签 |
| `#done` | 4 | `#Status/Done` | 🔴 高 | 已有标准标签 |
| `#Status/Review` | 52 | `#Status/Review` | ✅ 保留 | 已符合规范 |
| `#Status/TODO` | 37 | `#Status/TODO` | ✅ 保留 | 已符合规范 |
| `#Status/Done` | 31 | `#Status/Done` | ✅ 保留 | 已符合规范 |

### Type 标签映射

| 旧标签 | 出现次数 | 新标签 | 优先级 | 说明 |
|--------|----------|--------|--------|------|
| `#note` | 5 | `#Type/Note` | 🔴 高 | 已有标准标签 |
| `#Project` | 5 | `#Type/Project` | 🔴 高 | 已有标准标签 |
| `#MOC` | 5 | `#Type/MOC` | 🔴 高 | 已有标准标签 |
| `#reference` | 4 | `#Type/Reference` | 🔴 高 | 已有标准标签 |
| `#permanent-note` | 4 | `#Type/Note` | 🟡 中 | Zettelkasten 原则 |
| `#Type/Reference` | 44 | `#Type/Reference` | ✅ 保留 | 已符合规范 |
| `#Type/Note` | 32 | `#Type/Note` | ✅ 保留 | 已符合规范 |

---

## 📐 第三阶段：标签格式统一

### YAML frontmatter 格式标准化

**当前问题**：标签格式不一致
- 有的用 `#` 前缀：`tags: [#Type/Note]`
- 有的不用：`tags: [Type/Note]`
- 有的用数组：`tags: ["Type/Note"]`

**标准格式**：
```yaml
---
tags:
  - Domain/AI
  - Type/Note
  - Status/Active
---
```

或者（单行格式）：
```yaml
---
tags: [Domain/AI, Type/Note, Status/Active]
---
```

**规则**：
1. ✅ 不使用 `#` 前缀（在 YAML 中）
2. ✅ 使用数组格式
3. ✅ 标签按类型排序：Domain → Status → Type
4. ✅ 正文中的标签保留 `#` 前缀

---

## 🎯 执行脚本模板

### Python 脚本示例

```python
#!/usr/bin/env python3
"""
标签规范化脚本
用于批量替换 Obsidian 笔记中的标签
"""

import re
from pathlib import Path

# 标签映射表
TAG_MAPPINGS = {
    # 删除无效标签
    "#Domain/<SubDomain>": None,
    "#Status/<State>": None,
    "#Type/<ContentType>": None,
    "#Domain/": None,
    "#Type/": None,
    "#Status/": None,
    "#6f9": None,
    "#333": None,

    # Domain 标签映射
    "#AI": "#Domain/AI",
    "#Domain/Cognition": "#Domain/Cognitive",
    "#Domain/CognitiveSystem": "#Domain/Cognitive",
    "#Domain/ContentCreation": "#Domain/Content",
    "#SubDomain/IELTS": "#Domain/Language/IELTS",
    "#Python": "#Domain/Technology/Python",
    "#Architecture": "#Domain/TechStack/SystemDesign",
    "#SystemDesign": "#Domain/TechStack/SystemDesign",
    "#Tech/AI": "#Domain/AI",
    "#OpenSource": "#Domain/Technology/OpenSource",
    "#ContentCreation": "#Domain/Content",
    "#card": "#Type/Card",
    "#Domain/Psychology": "#Domain/Cognitive/Psychology",
    "#Domain/MentalModel": "#Domain/Cognitive/MentalModel",
    "#Concurrency": "#Domain/TechStack/Concurrency",
    "#CloudNative": "#Domain/TechStack/CloudNative",
    "#CareerPlanning": "#Domain/Career/Planning",
    "#EngineeringMindset": "#Domain/Career/Engineering",

    # Status 标签映射
    "#todo": "#Status/TODO",
    "#done": "#Status/Done",

    # Type 标签映射
    "#note": "#Type/Note",
    "#Project": "#Type/Project",
    "#MOC": "#Type/MOC",
    "#reference": "#Type/Reference",
    "#permanent-note": "#Type/Note",
}

def normalize_tags_in_file(file_path: Path):
    """规范化单个文件中的标签"""
    content = file_path.read_text(encoding='utf-8')
    original_content = content

    # 替换正文中的标签
    for old_tag, new_tag in TAG_MAPPINGS.items():
        if new_tag is None:
            # 删除标签
            content = re.sub(r'\b' + re.escape(old_tag) + r'\b', '', content)
        else:
            # 替换标签
            content = re.sub(r'\b' + re.escape(old_tag) + r'\b', new_tag, content)

    # 如果内容有变化，写回文件
    if content != original_content:
        file_path.write_text(content, encoding='utf-8')
        print(f"✅ 已更新: {file_path}")
        return True
    return False

def main():
    """主函数"""
    # 扫描笔记目录
    note_dirs = [
        Path("0.DailyNotes"),
        Path("1.Projects"),
        Path("2.Topics"),
        Path("3.Resources"),
        Path("4.Archives"),
        Path("6.Calendar"),
    ]

    total_files = 0
    updated_files = 0

    for note_dir in note_dirs:
        if not note_dir.exists():
            continue

        for md_file in note_dir.rglob("*.md"):
            total_files += 1
            if normalize_tags_in_file(md_file):
                updated_files += 1

    print(f"\n📊 统计:")
    print(f"  扫描文件: {total_files}")
    print(f"  更新文件: {updated_files}")
    print(f"  更新率: {updated_files/total_files*100:.1f}%")

if __name__ == "__main__":
    main()
```

### Templater 脚本示例

```javascript
<%*
// 标签规范化 Templater 脚本
// 文件位置: 5.Misc/Template/Templater/tag-normalization.md

const tagMappings = {
  // Domain 标签
  "#AI": "#Domain/AI",
  "#Python": "#Domain/Technology/Python",
  // ... 其他映射
};

let content = await app.vault.read(this.file);

// 替换标签
for (const [oldTag, newTag] of Object.entries(tagMappings)) {
  if (newTag) {
    content = content.replaceAll(oldTag, newTag);
  } else {
    content = content.replaceAll(oldTag, "");
  }
}

await app.vault.modify(this.file, content);
%>
```

---

## ✅ 验证检查清单

执行完标签替换后，使用以下命令验证：

```bash
# 1. 检查是否还有旧标签
grep -r "#AI" --include="*.md" 0.DailyNotes 1.Projects 2.Topics 3.Resources 4.Archives 6.Calendar

# 2. 检查是否还有占位符标签
grep -r "#Domain/<SubDomain>" --include="*.md" .

# 3. 统计标签使用情况
grep -rh "^tags:" --include="*.md" | sort | uniq -c | sort -rn | head -20

# 4. 检查标签格式一致性
grep -rh "tags:.*#" --include="*.md" | wc -l  # 应该为 0
```

---

## 📊 进度跟踪

- [x] Phase 1: 标签审计完成
- [ ] Phase 2: 删除无效标签（待执行）
- [ ] Phase 3: 批量替换旧标签（待执行）
- [ ] Phase 4: 格式统一（待执行）
- [ ] Phase 5: 验证与收尾（待执行）

---

## 📝 变更日志

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-01-25 | v1.0 | 初始版本，完成标签审计和映射表建立 |

---

**相关文档**:
- [标签规范化审计报告_20260125.md](标签规范化审计报告_20260125.md)
- [仓库标签管理系统.md](仓库标签管理系统.md)
- [GitHub Issue #1](https://github.com/duanbiao2000/obsidianDoc/issues/1)
