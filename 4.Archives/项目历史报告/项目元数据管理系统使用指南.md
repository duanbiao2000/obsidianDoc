# 项目元数据管理系统 - 使用指南

## 📋 系统概述

本系统提供了一套完整的项目元数据管理工具，包括：
1. **元数据审计** - 扫描和识别缺失的元数据字段
2. **自动补充** - 批量为项目添加 `update` 字段
3. **活跃度监控** - 识别需要归档的不活跃项目
4. **自动化监控** - 集成到 Weekly Review 流程

---

## 🚀 快速开始

### Phase 1: 元数据审计

**脚本**: `5.Misc/Template/Scripts/project_metadata_audit.py`

**功能**:
- 扫描 `1.Projects/` 目录下所有项目文件
- 检查 YAML frontmatter 完整性
- 生成详细的审计报告

**使用方法**:
```bash
cd obsidianDoc-issue3
python 5.Misc/Template/Scripts/project_metadata_audit.py
```

**输出**: `项目元数据审计报告_Phase1.md`

---

### Phase 2: 补充 update 字段

**脚本**: `5.Misc/Template/Scripts/add_update_field.py`

**功能**:
- 批量为缺少 `update` 字段的文件添加当前日期
- 支持 dry-run 模式预览修改
- 自动保留原有 YAML 格式

**使用方法**:
```bash
cd obsidianDoc-issue3
python 5.Misc/Template/Scripts/add_update_field.py
```

**注意**:
- 脚本会先执行 dry-run，预览所有修改
- 确认无误后才会实际修改文件
- 建议在执行前提交 Git，方便回滚

---

### Phase 3: 项目活跃度检查

**脚本**: `5.Misc/Template/Scripts/check_project_activity.py`

**功能**:
- 基于 `update` 字段计算项目活跃度
- 识别超过 90 天未更新的项目
- 生成归档建议报告

**使用方法**:
```bash
cd obsidianDoc-issue3
python 5.Misc/Template/Scripts/check_project_activity.py
```

**输出**: `项目活跃度审计报告_Phase3.md`

**分类标准**:
- 🟢 **活跃项目**: 63天内更新
- 🟡 **预警项目**: 63-90天未更新
- 🔴 **不活跃项目**: 超过90天未更新

---

### Phase 4: 自动化监控（Templater 脚本）

**脚本**: `5.Misc/Template/Templater/project-activity-monitor.md`

**功能**:
- 在 Weekly Review 中自动生成项目活跃度报告
- 自动创建归档任务（Tasks 插件语法）
- 可视化展示项目健康度

#### 集成到 Weekly Review 模板

1. **打开 Weekly Review 模板**:
   ```
   5.Misc/Template/每周回顾模板.md
   ```

2. **添加以下内容**:
   ````markdown
   ## 项目管理

   <% tp.file.include('[[5.Misc/Template/Templater/project-activity-monitor]]') %>
   ````

3. **使用效果**:
   - 每次创建 Weekly Review 时自动运行
   - 显示项目活跃度统计
   - 自动生成归档任务
   - 标记需要关注的预警项目

#### 手动运行（不依赖模板）

在任何笔记中添加：
````markdown
<%*
// 复制 project-activity-monitor.md 的代码到这里
// 或使用 tp.file.include()
%>
````

---

## 📊 审计报告说明

### Phase 1 报告字段

| 字段 | 说明 |
|------|------|
| `has_update` | 是否有 update 字段 |
| `has_created` | 是否有 created 字段 |
| `has_tags` | 是否有 tags 字段 |
| `has_status` | 是否有 status 字段 |

### Phase 3 报告分类

| 类别 | 标准 | 行动 |
|------|------|------|
| 活跃项目 | 63天内更新 | ✅ 无需操作 |
| 预警项目 | 63-90天未更新 | ⚠️ 确认是否继续 |
| 不活跃项目 | 超过90天未更新 | ❌ 归档到 Archives |

---

## 🛠️ 维护工作流

### 日常维护

1. **Weekly Review**（每周）:
   - 运行 `project-activity-monitor` 脚本
   - 处理预警项目
   - 归档不活跃项目

2. **项目创建**（新建项目时）:
   - 确保包含完整的 YAML frontmatter
   - 必需字段: `update`, `created`, `tags`

### 季度审计

1. **运行完整审计**:
   ```bash
   python 5.Misc/Template/Scripts/project_metadata_audit.py
   python 5.Misc/Template/Scripts/check_project_activity.py
   ```

2. **批量补充元数据**:
   ```bash
   python 5.Misc/Template/Scripts/add_update_field.py
   ```

3. **归档不活跃项目**:
   - 移动到 `4.Archives/Projects/`
   - 更新 `1.Projects/_Index_of_1.Projects.md`

---

## 🔧 脚本自定义

### 修改不活跃阈值

编辑 `check_project_activity.py`:
```python
days_threshold = 90  # 改为其他值，如 60 或 120
```

编辑 `project-activity-monitor.md`:
```javascript
const daysThreshold = 90; // 改为其他值
```

### 修改监控目录

编辑 `project_metadata_audit.py`:
```python
projects_dir = '1.Projects'  # 改为其他目录
```

---

## 📝 常见问题

### Q: 为什么有些文件显示 `update: null`？

A: 这些文件有 `update` 字段，但值为 `null`。需要手动修复为有效日期（如 `update: 2025-12-06`）。

### Q: 如何排除某些项目不被归档？

A: 在项目文件中添加 `archive: false` 字段，修改脚本检查该字段。

### Q: Templater 脚本不工作？

A: 确保：
1. 已安装 Obsidian Templater 插件
2. Templater 插件已启用
3. 脚本文件路径正确

---

## 🎯 成功指标

- ✅ **100% 项目**有 `update` 字段
- ✅ **0 个不活跃项目**超过 180 天未更新
- ✅ **Weekly Review**自动生成活跃度报告
- ✅ **归档流程**自动化

---

## 📚 相关文档

- [仓库标签管理系统](../../Atlas/Index/仓库标签管理系统.md)
- [Whole Vault任务管理](../../Atlas/Index/Whole Vault任务管理.md)
- [Weekly Review 模板](./每周回顾模板.md)

---

**最后更新**: 2026-01-25
**维护者**: Claude Code Agent
**版本**: v1.0
