---
tags:
  - Type/MOC
  - dataview
  - project-management
description: 项目活跃度看板 - 使用 Dataview 实时展示项目状态
---

# 项目活跃度看板

> **说明**：本看板使用 Dataview 插件实时查询项目活跃度，数据来源于各项目的 `update` 字段。

---

## 📊 总体统计

```dataview
TABLE rows.file.link AS "项目数"
FROM "1.Projects"
WHERE !contains(file.path, "_Index_") AND !contains(file.path, "CLAUDE.md")
FLATTEN file.lists AS L
WHERE L.update AND duration(date(today) - date(L.update)).days <= 90
GROUP BY true
```

## 🟢 活跃项目（63天内更新）

```dataview
TABLE without ID
  file.link AS "项目",
  update AS "最后更新",
  date(update).format("YYYY-MM-DD") AS "更新日期",
  round(duration(date(today) - date(update)).days) + " 天前" AS "活跃度"
FROM "1.Projects"
WHERE !contains(file.path, "_Index_") AND !contains(file.path, "CLAUDE.md")
  AND update AND date(update) >= date(today) - dur(63 days)
SORT update DESC
```

## 🟡 预警项目（63-90天未更新）

```dataview
TABLE without ID
  file.link AS "项目",
  update AS "最后更新",
  date(update).format("YYYY-MM-DD") AS "更新日期",
  round(duration(date(today) - date(update)).days) + " 天前" AS "不活跃天数"
FROM "1.Projects"
WHERE !contains(file.path, "_Index_") AND !contains(file.path, "CLAUDE.md")
  AND update AND date(update) < date(today) - dur(63 days)
  AND date(update) >= date(today) - dur(90 days)
SORT update ASC
```

## 🔴 不活跃项目（超过90天未更新）

```dataview
TABLE without ID
  file.link AS "项目",
  update AS "最后更新",
  date(update).format("YYYY-MM-DD") AS "更新日期",
  round(duration(date(today) - date(update)).days) + " 天前" AS "不活跃天数",
  "📦 建议归档" AS "行动"
FROM "1.Projects"
WHERE !contains(file.path, "_Index_") AND !contains(file.path, "CLAUDE.md")
  AND update AND date(update) < date(today) - dur(90 days)
SORT update ASC
```

## ⚪ 无元数据项目

```dataview
TABLE without ID
  file.link AS "项目",
  "❌ 缺少 update 字段" AS "状态",
  "需手动补充" AS "行动"
FROM "1.Projects"
WHERE !contains(file.path, "_Index_") AND !contains(file.path, "CLAUDE.md")
  AND (!update OR update = null)
SORT file.name ASC
```

---

## 📈 活跃度趋势

### 项目分布饼图

```dataview
TABLE without ID
  "🟢 活跃" AS "状态",
  length(filter(rows, (r) => r.update AND date(r.update) >= date(today) - dur(63 days))) AS "数量"
FROM "1.Projects"
WHERE !contains(file.path, "_Index_") AND !contains(file.path, "CLAUDE.md")
GROUP BY true
```

### 最近7天更新的项目

```dataview
LIST
FROM "1.Projects"
WHERE !contains(file.path, "_Index_") AND !contains(file.path, "CLAUDE.md")
  AND update AND date(update) >= date(today) - dur(7 days)
SORT update DESC
```

---

## 🔧 配置说明

### 活跃度分类标准

| 类别 | 阈值 | 颜色 | 行动建议 |
|------|------|------|----------|
| 🟢 活跃项目 | ≤ 63 天更新 | 绿色 | 继续推进 |
| 🟡 预警项目 | 63-90 天未更新 | 黄色 | 关注确认 |
| 🔴 不活跃项目 | > 90 天未更新 | 红色 | 建议归档 |
| ⚪ 无元数据 | 缺少 update 字段 | 灰色 | 补充元数据 |

### 自定义阈值

如需修改活跃度阈值，编辑上述查询中的天数：
- **活跃阈值**: `dur(63 days)` → 改为其他值
- **归档阈值**: `dur(90 days)` → 改为其他值

---

## 🚀 使用方法

### 方式 1: 直接查看（推荐）
1. 打开本文件：`项目活跃度看板.md`
2. Dataview 会自动加载最新数据
3. 点击项目名称可跳转到对应文件

### 方式 2: 在其他笔记中引用
```markdown
![[Atlas/Index/项目活跃度看板]]
```

### 方式 3: 创建快捷方式
在 Obsidian 左侧栏收藏本文件，快速访问

---

## 📋 维护检查清单

- [ ] **每周**: 查看预警项目，确认是否继续
- [ ] **每月**: 运行完整元数据审计
- [ ] **每季度**: 归档不活跃项目
- [ ] **新建项目时**: 确保包含 `update` 字段

---

## 🔗 相关链接

- [项目元数据审计报告](../../项目元数据审计报告_Phase1.md)
- [项目活跃度审计报告](../../项目活跃度审计报告_Phase3.md)
- [项目元数据管理系统使用指南](../../项目元数据管理系统使用指南.md)
- [自动化监控脚本](../Template/Templater/project-activity-monitor.md)

---

**最后更新**: 2026-01-25
**依赖插件**: Dataview
**查询范围**: `1.Projects/` 目录
