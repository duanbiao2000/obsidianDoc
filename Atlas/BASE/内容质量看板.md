---
tags:
  - Domain/知识管理
  - Status/Done
  - Type/监控看板
created: 2026-01-25
update: 2026-01-25
rating: 5
view-count: 0
related:
  - [[Atlas/BASE/内容质量评估标准.md]]
  - [[Atlas/BASE/Whole Vault任务管理.md]]
  - [[Atlas/Index/仓库标签管理系统.md]]
cssclass: dashboard-quality
---

## 🔗 相关链接

**上级索引**:
- [[Atlas/BASE/_Index_of_BASE.md|BASE]]
- [[Atlas/MOCs.md|MOCs]]

---

## 🎯 质量看板总览

> 本看板用于监控知识库内容质量，展示质量分布统计、改进进度和趋势分析。

**数据更新时间**: `=this.update`

---

## 📊 全局质量指标

### 整体质量分布

```dataview
TABLEWithoutID
  "**📊 " + split(t, "/")[1] + "**" AS "质量等级",
  length(filter(rows, (r) => contains(r.tags, t))) AS "笔记数量",
  round(length(filter(rows, (r) => contains(r.tags, t))) / length(rows) * 100, 1) + "%" AS "占比"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
FLATTEN tags AS t
WHERE contains(t, "Quality/")
GROUP BY t
```

### 平均质量分统计

```dataview
TABLEWithoutID
  round(average(float(rating)), 2) AS "平均质量分",
  length(rows) AS "总笔记数",
  length(rows[rating >= 4.5]) AS "优秀笔记数",
  length(rows[rating >= 4.0 AND rating < 4.5]) AS "良好笔记数",
  length(rows[rating >= 3.0 AND rating < 4.0]) AS "合格笔记数",
  length(rows[rating < 3.0]) AS "需改进笔记数",
  round(length(rows[rating >= 4.0]) / length(rows) * 100, 1) + "%" AS "达标率"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE rating
```

---

## 🎯 质量目标进度

### 目标 1: 80% 的笔记质量评分 ≥ 4 分

```dataview
TABLEWithoutID
  round(length(rows[rating >= 4.0]) / length(rows) * 100, 1) + "%" AS "当前进度",
  "80%" AS "目标",
  round((80 - (length(rows[rating >= 4.0]) / length(rows) * 100)), 1) + "%" AS "差距",
  round((length(rows[rating >= 4.0]) / length(rows) * 100) / 80 * 100, 1) + "%" AS "完成度"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE rating
```

**目标状态**:
- [ ] 🎯 **已完成** (≥ 80%)
- [ ] 🚀 **进行中** (60-79%)
- [ ] ⚠️ **需加速** (< 60%)

### 目标 2: 所有新笔记使用质量模板

```dataview
TABLE
  file.link AS "新笔记",
  dateformat(date(created), "yyyy-MM-dd") AS "创建日期",
  rating AS "质量评分",
  tags AS "质量标签"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE date(created) >= date(today) - dur(30 days)
SORT created DESC
LIMIT 20
```

---

## 🔥 高优先级改进任务

### 高访问量低质量笔记（需优先处理）

```dataview
TABLE
  file.link AS "笔记",
  rating AS "当前评分",
  view-count AS "访问次数",
  dateformat(date(created), "yyyy-MM-dd") AS "创建日期",
  file.folder AS "目录"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE rating AND rating < 3 AND view-count > 5
SORT view-count DESC
LIMIT 20
```

**行动建议**:
1. 立即改进（1 周内完成）
2. 使用 [[5.Misc/Template/质量评估模板.md]] 进行详细评估
3. 改进后更新 YAML `rating` 字段
4. 标记为 `#Quality/Good` 或更高等级

### 技术栈低质量笔记

```dataview
TABLE
  file.link AS "笔记",
  rating AS "当前评分",
  dateformat(date(update), "yyyy-MM-dd") AS "最后更新",
  tags AS "标签"
FROM "2.Topics/01.技术栈"
WHERE rating AND rating < 3
SORT rating ASC, update DESC
LIMIT 20
```

**行动建议**:
1. 补充"踩坑记录"和"最佳实践"
2. 添加实际代码示例
3. 2 周内完成改进

---

## 📈 质量趋势分析

### 按月统计（新创建笔记的平均质量）

```dataview
TABLEWithoutID
  key AS "月份",
  round(rows.rating.average(), 2) AS "平均质量分",
  length(rows) AS "新增笔记数"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE rating
FLATTEN dateformat(date(created), "yyyy-MM") AS key
GROUP BY key
SORT key DESC
LIMIT 12
```

### 按目录统计

```dataview
TABLEWithoutID
  file.folder AS "目录",
  round(average(float(rating)), 2) AS "平均质量分",
  length(rows) AS "笔记总数",
  length(rows[rating >= 4.0]) AS "达标笔记数",
  round(length(rows[rating >= 4.0]) / length(rows) * 100, 1) + "%" AS "达标率"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE rating
GROUP BY file.folder
SORT average(rating) DESC
```

---

## ✅ 本月质量审计进度

### 待评估笔记（未评分）

```dataview
TABLE
  file.link AS "笔记",
  view-count AS "访问次数",
  dateformat(date(created), "yyyy-MM-dd") AS "创建日期",
  file.folder AS "目录"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE !rating
SORT view-count DESC
LIMIT 30
```

**审计目标**: 本月完成 10% 笔记的质量评估

**当前进度**:
```dataview
TABLEWithoutID
  length(rows) AS "未评分笔记数",
  "待评估" AS "状态",
  round(length(rows) * 0.1, 0) AS "本月目标数"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE !rating
```

---

## 🏆 每月最佳笔记

### 优秀笔记展示（质量评分 ≥ 4.5）

```dataview
LIST
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE rating AND rating >= 4.5
SORT rating DESC, view-count DESC
LIMIT 10
```

**本月最佳笔记评选标准**:
1. 质量评分 ≥ 4.5 分
2. 访问次数 ≥ 5 次
3. 本月创建或更新
4. 可作为其他笔记的范例

---

## 📋 质量改进任务清单

### 本月任务

```tasks
#质量改进
not done
due before this month + 1month
sort by priority
sort by due
limit 20
```

### 已完成任务

```tasks
#质量改进
done
done after last month
sort by completed
limit 20
```

---

## 🔍 快速查询

### 查询所有低质量笔记

```dataview
TABLE
  file.link AS "笔记",
  rating AS "评分",
  view-count AS "访问次数",
  dateformat(date(created), "yyyy-MM-dd") AS "创建日期",
  file.folder AS "目录"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE rating AND rating < 3
SORT rating ASC, view-count DESC
```

### 查询所有未评分笔记

```dataview
TABLE
  file.link AS "笔记",
  view-count AS "访问次数",
  dateformat(date(created), "yyyy-MM-dd") AS "创建日期",
  file.folder AS "目录"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE !rating
SORT view-count DESC
```

### 查询最近改进的笔记

```dataview
TABLE
  file.link AS "笔记",
  rating AS "当前评分",
  dateformat(date(update), "yyyy-MM-dd") AS "更新日期",
  tags AS "质量标签"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE rating AND contains(tags, "Quality/") AND date(update) >= date(today) - dur(30 days)
SORT update DESC
```

---

## 📊 质量分布图表

### 按质量等级分布

```dataview
TABLEWithoutID
  split(t, "/")[1] AS "质量等级",
  length(filter(rows, (r) => contains(r.tags, t))) AS "笔记数量"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
FLATTEN tags AS t
WHERE contains(t, "Quality/")
GROUP BY t
SORT split(t, "/")[1] DESC
```

### 按评分段分布

```dataview
TABLEWithoutID
  "**" + key + "**" AS "评分段",
  length(rows) AS "笔记数量"
FROM "1.Projects" OR "2.Topics" OR "3.Resources"
WHERE rating
FLATTEN
  choose(
    number(rating) >= 4.5 ? 1 :
    number(rating) >= 4.0 ? 2 :
    number(rating) >= 3.0 ? 3 :
    number(rating) >= 2.0 ? 4 : 5,
    "4.5-5.0 优秀",
    "4.0-4.4 良好",
    "3.0-3.9 合格",
    "2.0-2.9 需改进",
    "0-1.9 不合格"
  ) AS key
GROUP BY key
SORT key DESC
```

---

## 🎯 行动计划

### 本月重点任务

- [ ] **任务 1**: 完成本月 10% 笔记的质量评估
  - 目标数量: `=round(length(rows) * 0.1, 0)` 篇
  - 截止日期: 本月底
  - 使用模板: [[5.Misc/Template/质量评估模板.md]]

- [ ] **任务 2**: 改进所有高访问量低质量笔记
  - 目标数量: 所有 view-count > 5 且 rating < 3 的笔记
  - 截止日期: 2 周内
  - 优先级: 🔴 高

- [ ] **任务 3**: 评选本月最佳笔记
  - 目标: 选出 3-5 篇最佳笔记
  - 截止日期: 本月底
  - 展示位置: 本看板"每月最佳笔记"部分

- [ ] **任务 4**: 更新质量标准文档
  - 根据实际使用情况调整评分标准
  - 截止日期: 本月底
  - 文档位置: [[Atlas/BASE/内容质量评估标准.md]]

### 下月计划

- [ ] 继续推进质量评估（目标: 累计完成 30% 笔记评分）
- [ ] 建立质量改进跟踪机制
- [ ] 收集用户反馈，优化评估流程
- [ ] 开展质量标准培训

---

## 📚 相关文档

- **质量标准**: [[Atlas/BASE/内容质量评估标准.md]]
- **评估模板**: [[5.Misc/Template/质量评估模板.md]]
- **任务管理**: [[Atlas/BASE/Whole Vault任务管理.md]]
- **标签管理**: [[Atlas/Index/仓库标签管理系统.md]]

---

## 💡 使用提示

### 每月工作流

1. **月初**: 查看本月质量审计目标（10% 笔记）
2. **月中**: 使用 Dataview 查询生成待评估笔记列表
3. **评估**: 使用质量评估模板进行评分
4. **改进**: 对低质量笔记进行改进
5. **月末**: 统计本月质量改进情况，更新看板

### 定期回顾

- **每周**: 查看高优先级改进任务进度
- **每月**: 查看质量趋势分析，调整改进策略
- **每季度**: 全面回顾质量管理体系，更新标准

---

**目标**: 建立可持续的质量监控机制，确保知识库内容质量持续提升。
