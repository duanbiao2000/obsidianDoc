---
tags:
  - Domain/Tech/Obsidian
  - Type/Reference
  - plugin-guide
update: 2026-01-26
related:
  - [[../../Index/仓库标签管理系统]]
  - [[../../BASE/插件使用指南]]
---

# Zoottelkeeper 使用指南

> Zoottelkeeper 是一个自动生成目录索引的 Obsidian 插件
>
> 作者: [foambubble](https://github.com/foambubble/zoottelkeeper)
> 插件主页: [GitHub](https://github.com/foambubble/zoottelkeeper)

---

## 🎯 插件简介

Zoottelkeeper 可以自动为目录生成索引文件,列出该目录下的所有文件和子目录。

### 核心功能
- ✅ 自动生成目录索引
- ✅ 支持嵌套目录
- ✅ 可配置的文件夹排除/包含规则
- ✅ 自动更新索引内容
- ✅ 支持文件图标和前缀

---

## ⚙️ 当前配置

### 配置文件位置
`.obsidian/plugins/zoottelkeeper-obsidian-plugin/data.json`

### 关键配置项

```json
{
  "indexPrefix": "_Index_of_",
  "indexHeaderLevel": 1,
  "excludeSubdirectory": "[]",
  "foldersExcluded": "4.Archives/*\n6.Calendar/*\n.sisyphus/*\n.obsidian/*",
  "foldersIncluded": "0.DailyNotes/\n1.Projects/\n2.Topics/\n3.Resources/\n5.Misc/\nAtlas/",
  "hideNoteInIndex": false,
  "showNoteType": true,
  "firstRun": false
}
```

### 配置说明

| 配置项 | 说明 | 当前值 |
|--------|------|--------|
| **indexPrefix** | 索引文件名前缀 | `_Index_of_` |
| **foldersExcluded** | 排除的目录(不生成索引) | `4.Archives/*`, `6.Calendar/*` 等 |
| **foldersIncluded** | 包含的目录(生成索引) | `0.DailyNotes/`, `2.Topics/` 等 |
| **showNoteType** | 显示文件类型图标 | ✅ 启用 |
| **hideNoteInIndex** | 在索引中隐藏自身 | ❌ 禁用 |

---

## 🚀 使用方法

### 方法1: 为当前目录创建索引

1. 在文件浏览器中打开任意目录
2. 打开命令面板 (Ctrl/Cmd + P)
3. 输入 "Zoottelkeeper: Create index for current folder"
4. 插件会自动创建/更新 `_Index_of_<目录名>.md`

### 方法2: 重新生成所有索引

1. 打开命令面板
2. 输入 "Zoottelkeeper: Recreate all indexes"
3. 插件会遍历所有配置的目录并生成/更新索引

**⚠️ 注意**: 重新生成会覆盖现有索引内容,但会保留 Zoottelkeeper 标记外的手动添加内容

---

## 📝 索引文件结构

### 标准索引文件格式

```markdown
---
tags:
  - Type/Index
  - Domain/<Category>
---

# 目录名 索引

## 📊 统计概览
(可自定义 Dataview 查询)

## 📁 自动索引

%% Zoottelkeeper: Beginning of the autogenerated index file list  %%
📄 [[文件1]]
🗂️ [[子目录1]]
%% Zoottelkeeper: End of the autogenerated index file list  %%
```

### 关键标记

```markdown
%% Zoottelkeeper: Beginning of the autogenerated index file list  %%
%% Zoottelkeeper: End of the autogenerated index file list  %%
```

这两个标记之间的内容由 Zoottelkeeper 自动生成,不要手动编辑。

---

## 🎨 自定义索引内容

### 添加统计概览

在 Zoottelkeeper 标记之前添加 Dataview 查询:

```markdown
## 📊 统计概览
```dataviewjs
const pages = dv.pages('"当前目录路径"');
dv.paragraph(`总笔记数: ${pages.length}`);
```
```

### 添加手动维护的内容

在 Zoottelkeeper 标记之外添加内容,这些内容不会被自动覆盖:

```markdown
%% Zoottelkeeper: Beginning %%
(自动生成的内容)
%% Zoottelkeeper: End %%

## 📌 重点笔记
(手动维护的重点笔记列表,不会被覆盖)
```

---

## 🔍 故障排除

### 索引未生成

**症状**: 运行命令后没有创建索引文件

**可能原因**:
1. 目录在 `foldersExcluded` 列表中
2. 目录不在 `foldersIncluded` 列表中
3. 插件未正确安装

**解决方法**:
1. 检查插件配置,确认目录在 `foldersIncluded` 中
2. 重新加载插件(禁用→启用)
3. 检查 Obsidian 控制台是否有错误信息

### 索引不更新

**症状**: 添加新文件后索引没有更新

**解决方法**:
1. 手动运行 "Zoottelkeeper: Recreate all indexes"
2. 或在命令面板中运行 "Zoottelkeeper: Create index for current folder"

### 索引格式错误

**症状**: 索引内容格式混乱

**可能原因**:
- 手动编辑了 Zoottelkeeper 标记之间的内容
- 配置冲突

**解决方法**:
1. 恢复到 Git 历史中的正确版本
2. 重新运行 "Recreate all indexes"

---

## 📋 最佳实践

### 1. 保持一致的命名规范

所有目录索引文件命名为: `_Index_of_<目录名>.md`

### 2. 不要手动编辑自动生成部分

Zoottelkeeper 标记之间的内容会被覆盖,如需自定义内容,在标记之外添加

### 3. 定期检查索引完整性

使用 [[索引健康报告]] 定期检查索引覆盖率

### 4. 合理配置包含/排除规则

- **包含**: 需要索引的活跃目录
- **排除**: Archives、临时文件、系统目录

### 5. 与 Dataview 结合使用

在索引文件中添加 Dataview 统计模块,提供实时数据

---

## 🔗 相关工具

### 与 Zoottelkeeper 配合使用的插件

1. **Dataview**
   - 在索引中添加统计概览
   - 动态查询目录内容

2. **Templater**
   - 自动化索引创建流程
   - 自定义索引模板

3. **obsidian-git**
   - 自动提交索引更新
   - 保持版本控制

---

## 📚 相关文档

- [[Dataview查询模板集]] - 常用 Dataview 查询示例
- [[索引健康报告]] - 索引系统监控
- [[仓库标签管理系统]] - 标签规范化指南
- [[../../Index/Dataviewer]] - Dataview 使用示例

---

## 📖 更新日志

| 日期 | 更新内容 |
|------|---------|
| 2026-01-26 | 创建使用指南文档 |
| 2026-01-26 | 配置文件夹排除规则(4.Archives, 6.Calendar) |
