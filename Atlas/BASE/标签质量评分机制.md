# 标签质量评分机制

> 系统化评估和提升知识库标签质量的评分体系
> 创建时间: 2026-01-26
> 相关: 知识库优化线路图 P2-4 - 建立标签质量评分机制

---

## 📋 目录

- [评分体系概述](#评分体系概述)
- [评分维度详解](#评分维度详解)
- [质量等级标准](#质量等级标准)
- [自动化评分脚本](#自动化评分脚本)
- [质量提升建议](#质量提升建议)

---

## 评分体系概述

### 标签质量的重要性

**高质量的标签体系能够**:
- **提升检索效率**: 快速找到相关笔记
- **增强知识发现**: 通过标签关联发现隐藏的知识连接
- **降低维护成本**: 减少手动整理和清理工作
- **支持自动化**: 为智能推荐和分类提供基础

### 评分模型

标签质量采用 **100 分制** 评分,包含 5 个维度:

| 维度 | 权重 | 评估重点 |
|------|------|----------|
| **完整性** | 30 分 | 三层标签是否齐全 |
| **规范性** | 25 分 | 是否符合标签命名规范 |
| **准确性** | 20 分 | 标签与内容的相关性 |
| **一致性** | 15 分 | 与同类笔记标签的一致程度 |
| **实用性** | 10 分 | 标签的实际使用价值 |

---

## 评分维度详解

### 维度 1: 完整性 (30 分)

**评估标准**: 是否包含完整的三层标签体系

#### Domain 层 (10 分)
```
✅ 有 Domain 标签: +10 分
❌ 缺少 Domain 标签: 0 分
```

**Domain 标签示例**:
- `#Domain/AI`
- `#Domain/Cognitive`
- `#Domain/Tech/Stack`
- `#Domain/LanguageAndMigration`

#### Status 层 (10 分)
```
✅ 有 Status 标签: +10 分
❌ 缺少 Status 标签: 0 分
```

**Status 标签示例**:
- `#Status/TODO`
- `#Status/InProgress`
- `#Status/Review`
- `#Status/Done`

#### Type 层 (10 分)
```
✅ 有 Type 标签: +10 分
❌ 缺少 Type 标签: 0 分
```

**Type 标签示例**:
- `#Type/Note`
- `#Type/Reference`
- `#Type/Keynote`
- `#Type/SystemGuide`

---

### 维度 2: 规范性 (25 分)

**评估标准**: 标签是否符合知识库的命名规范

#### 格式规范 (10 分)
```
✅ 使用三层结构: +10 分
⚠️ 使用两层结构: +5 分
❌ 单层或无结构: 0 分
```

**正确示例**:
- ✅ `#Domain/Tech/Stack`
- ✅ `#Status/Review`
- ❌ `#tech`
- ❌ `#技术栈`

#### 命名约定 (10 分)
```
✅ 使用英文标签: +10 分
⚠️ 中英文混合: +5 分
❌ 纯中文标签: 0 分
```

**命名约定**:
- Domain: 英文,大写开头
- Status: 英文,大写开头
- Type: 英文,大写开头
- SubDomain/SubType: 英文

#### 标签数量 (5 分)
```
✅ 3-7 个标签: +5 分
⚠️ 8-12 个标签: +3 分
❌ >12 个或 <3 个: 0 分
```

---

### 维度 3: 准确性 (20 分)

**评估标准**: 标签是否准确反映笔记内容

#### 内容相关性 (10 分)
```
✅ 标签与内容高度相关: +10 分
⚠️ 部分标签相关: +5 分
❌ 标签与内容不符: 0 分
```

**评估方法**:
1. Domain 标签是否匹配笔记所在目录
2. Status 标签是否反映笔记的实际状态
3. Type 标签是否准确描述笔记类型

#### 误用检测 (10 分)
```
✅ 无误用标签: +10 分
⚠️ 1 个误用标签: -5 分
❌ ≥2 个误用标签: -10 分
```

**常见误用**:
- 在 `2.Topics` 中使用 `#Type/Task` (应在 `1.Projects`)
- 状态为已完成但使用 `#Status/TODO`
- 教程类笔记使用 `#Type/Reference` (应使用 `#Type/Tutorial`)

---

### 维度 4: 一致性 (15 分)

**评估标准**: 与同类笔记标签的一致程度

#### 同类笔记对比 (10 分)
```
✅ 与同类笔记标签高度一致: +10 分
⚠️ 部分一致: +5 分
❌ 标签差异较大: 0 分
```

**对比方法**:
1. 找到同目录下的其他笔记
2. 计算 Jaccard 相似度
3. 评估标签模式一致性

**示例**:
```
笔记 A: #Domain/AI #Type/Note #Status/Done
笔记 B: #Domain/AI #Type/Note #Status/Done
→ 相似度: 100% (高度一致)

笔记 A: #Domain/AI #Type/Note #Status/Done
笔记 B: #Domain/AI/ML #Type/Reference #Status/Review
→ 相似度: 33% (差异较大)
```

#### 层级一致性 (5 分)
```
✅ 标签层级符合深度: +5 分
❌ 层级过深或过浅: 0 分
```

**层级标准**:
- Domain: 2-3 层
- Status: 2 层
- Type: 2 层

---

### 维度 5: 实用性 (10 分)

**评估标准**: 标签的实际使用价值

#### 检索频率 (5 分)
```
✅ 标签用于检索 ≥5 次: +5 分
⚠️ 检索 1-4 次: +3 分
❌ 从未被检索: 0 分
```

#### 关联价值 (5 分)
```
✅ 帮助发现 ≥3 个相关笔记: +5 分
⚠️ 发现 1-2 个相关笔记: +3 分
❌ 无关联笔记: 0 分
```

---

## 质量等级标准

### 等级划分

| 分数区间 | 等级 | 描述 | 处理建议 |
|---------|------|------|----------|
| **90-100** | ⭐⭐⭐⭐⭐ 优秀 | 标签质量极高 | 作为范例,保持当前水平 |
| **75-89** | ⭐⭐⭐⭐ 良好 | 标签质量较好 | 微调即可 |
| **60-74** | ⭐⭐⭐ 中等 | 标签质量一般 | 需要改进 |
| **40-59** | ⭐⭐ 较差 | 标签质量较低 | 优先改进 |
| **0-39** | ⭐ 极差 | 标签质量极差 | 立即重构 |

### 等级分布目标

- **优秀 (≥90)**: ≥20%
- **良好 (≥75)**: ≥50%
- **中等 (≥60)**: ≥80%
- **较差 (<60)**: <20%
- **极差 (<40)**: <5%

---

## 自动化评分脚本

### Python 脚本: 标签质量评分器

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
标签质量评分器

功能: 自动评估笔记的标签质量
使用方法: python tag_quality_scorer.py <note_path>
"""

import sys
import re
from pathlib import Path
from typing import List, Tuple, Dict
from collections import defaultdict

class TagQualityScorer:
    def __init__(self, vault_root: str):
        self.vault_root = Path(vault_root)
        self.all_tags = self._scan_all_tags()
        self.directory_tags = self._build_directory_tags()

    def _scan_all_tags(self) -> Dict[str, int]:
        """扫描知识库中所有标签的使用频率"""
        tag_counts = defaultdict(int)
        for md_file in self.vault_root.rglob("*.md"):
            try:
                content = md_file.read_text(encoding='utf-8')
                tags = re.findall(r'#\w+/\w+', content)
                for tag in tags:
                    tag_counts[tag] += 1
            except Exception as e:
                print(f"警告: 无法读取 {md_file}: {e}")
        return dict(tag_counts)

    def _build_directory_tags(self) -> Dict[str, List[str]]:
        """构建各目录的常用标签"""
        dir_tags = defaultdict(lambda: defaultdict(int))
        for md_file in self.vault_root.rglob("*.md"):
            try:
                content = md_file.read_text(encoding='utf-8')
                tags = re.findall(r'#\w+/\w+', content)
                directory = str(md_file.parent)
                for tag in tags:
                    dir_tags[directory][tag] += 1
            except Exception:
                pass

        # 返回每个目录最常用的 3 个标签
        return {
            dir: sorted(tags.items(), key=lambda x: x[1], reverse=True)[:3]
            for dir, tags in dir_tags.items()
        }

    def score_note(self, note_path: str) -> Tuple[int, Dict]:
        """对单个笔记进行评分"""
        note_file = Path(note_path)
        if not note_file.exists():
            raise FileNotFoundError(f"笔记不存在: {note_path}")

        content = note_file.read_text(encoding='utf-8')

        # 提取标签
        tags = re.findall(r'(#\w+(?:/\w+)*)', content)
        tags_set = set(tags)

        # 评分
        scores = {
            'completeness': self._score_completeness(tags_set),
            'standardization': self._score_standardization(tags_set),
            'accuracy': self._score_accuracy(tags_set, note_file.parent, content),
            'consistency': self._score_consistency(tags_set, note_file.parent),
            'utility': self._score_utility(tags_set)
        }

        total_score = sum(scores.values())

        return total_score, scores

    def _score_completeness(self, tags: set) -> int:
        """评分: 完整性 (30 分)"""
        score = 0

        # Domain 标签
        has_domain = any(tag.startswith('#Domain/') for tag in tags)
        score += 10 if has_domain else 0

        # Status 标签
        has_status = any(tag.startswith('#Status/') for tag in tags)
        score += 10 if has_status else 0

        # Type 标签
        has_type = any(tag.startswith('#Type/') for tag in tags)
        score += 10 if has_type else 0

        return score

    def _score_standardization(self, tags: set) -> int:
        """评分: 规范性 (25 分)"""
        score = 0

        # 格式规范 (三层结构)
        three_layer_tags = [t for t in tags if t.count('/') >= 1]
        if three_layer_tags:
            score += 10
        elif len(tags) > 0:
            score += 5

        # 命名约定 (英文)
        english_tags = [t for t in tags if not re.search(r'[\u4e00-\u9fff]', t)]
        if len(english_tags) == len(tags):
            score += 10
        elif len(english_tags) > 0:
            score += 5

        # 标签数量 (3-7 个)
        tag_count = len(tags)
        if 3 <= tag_count <= 7:
            score += 5
        elif 0 < tag_count < 3 or tag_count <= 12:
            score += 3

        return score

    def _score_accuracy(self, tags: set, directory: Path, content: str) -> int:
        """评分: 准确性 (20 分)"""
        score = 0

        # Domain 与目录匹配 (简化版本)
        dir_str = str(directory)
        if '1.Projects' in dir_str:
            has_project_tag = any('Project' in t or 'IP' in t for t in tags)
            score += 5 if has_project_tag else 0
        elif '2.Topics' in dir_str:
            has_domain_tag = any(t.startswith('#Domain/') for t in tags)
            score += 5 if has_domain_tag else 0

        # Status 与内容匹配
        status_tags = [t for t in tags if t.startswith('#Status/')]
        if status_tags:
            status = status_tags[0]
            if 'Done' in status and '✅' in content:
                score += 5
            elif 'TODO' in status and ('TODO' in content or '待办' in content):
                score += 5
            else:
                score += 2

        # Type 与内容匹配 (简化版本)
        if any(t.startswith('#Type/') for t in tags):
            score += 5

        return min(score, 20)

    def _score_consistency(self, tags: set, directory: Path) -> int:
        """评分: 一致性 (15 分)"""
        score = 0

        dir_str = str(directory)
        if dir_str in self.directory_tags:
            common_tags = [tag for tag, _ in self.directory_tags[dir_str]]
            intersection = len(tags.intersection(common_tags))
            score += min(intersection * 3, 10)

        # 层级一致性
        proper_depth = all(1 <= tag.count('/') <= 2 for tag in tags)
        score += 5 if proper_depth else 0

        return score

    def _score_utility(self, tags: set) -> int:
        """评分: 实用性 (10 分)"""
        score = 0

        # 检索频率 (基于全局标签统计)
        if tags:
            avg_usage = sum(self.all_tags.get(tag, 0) for tag in tags) / len(tags)
            if avg_usage >= 5:
                score += 5
            elif avg_usage >= 1:
                score += 3

        # 关联价值 (简化: 基于标签出现频率)
        if tags:
            min_usage = min(self.all_tags.get(tag, 0) for tag in tags)
            if min_usage >= 3:
                score += 5
            elif min_usage >= 1:
                score += 3

        return score

    def get_grade(self, score: int) -> Tuple[str, str]:
        """获取质量等级"""
        if score >= 90:
            return "⭐⭐⭐⭐⭐ 优秀", "保持当前水平"
        elif score >= 75:
            return "⭐⭐⭐⭐ 良好", "微调即可"
        elif score >= 60:
            return "⭐⭐⭐ 中等", "需要改进"
        elif score >= 40:
            return "⭐⭐ 较差", "优先改进"
        else:
            return "⭐ 极差", "立即重构"

def main():
    if len(sys.argv) < 2:
        print("使用方法: python tag_quality_scorer.py <note_path> [vault_root]")
        sys.exit(1)

    note_path = sys.argv[1]
    vault_root = sys.argv[2] if len(sys.argv) > 2 else "."

    scorer = TagQualityScorer(vault_root)

    try:
        total_score, scores = scorer.score_note(note_path)
        grade, suggestion = scorer.get_grade(total_score)

        print(f"\n标签质量评分: {total_score}/100")
        print(f"质量等级: {grade}")
        print(f"改进建议: {suggestion}")
        print("\n详细得分:")
        for dimension, score in scores.items():
            print(f"  {dimension}: {score}")

    except Exception as e:
        print(f"错误: {e}")
        sys.exit(1)

if __name__ == "__main__":
    main()
```

---

## 质量提升建议

### 提升完整性的建议

1. **确保三层标签齐全**
   - 每个笔记至少包含 Domain, Status, Type 各一个
   - 使用 `tag_recommender.md` 自动推荐标签

2. **使用标签模板**
   ```yaml
   ---
   tags:
     - #Domain/<具体领域>
     - #Status/<状态>
     - #Type/<类型>
   ---
   ```

### 提升规范性的建议

1. **遵循命名约定**
   - 使用英文标签,大写开头
   - 采用分层结构 (最多 3 层)
   - 避免中文标签

2. **控制标签数量**
   - 最少 3 个,最多 12 个
   - 理想范围: 5-7 个标签

### 提升准确性的建议

1. **匹配笔记内容**
   - Domain 标签应反映笔记所属领域
   - Status 标签应反映实际完成状态
   - Type 标签应准确描述笔记类型

2. **定期审查**
   - 每月检查标签准确性
   - 更新过时的 Status 标签

### 提升一致性的建议

1. **参考同类笔记**
   - 查看同目录下其他笔记的标签
   - 保持标签模式一致

2. **使用标签推荐**
   - 创建笔记时使用 `tag_recommender.md`
   - 基于目录和内容自动推荐标签

### 提升实用性的建议

1. **优先使用高频标签**
   - 使用标签搜索频率高的标签
   - 避免创建过于特殊的标签

2. **增强标签关联**
   - 使用已有标签,避免创建新标签
   - 通过标签连接相关笔记

---

## 批量评分与报告

### 生成全库标签质量报告

```bash
# 1. 扫描所有 Markdown 文件
find . -name "*.md" -type f > all_notes.txt

# 2. 批量评分
while read note; do
  python tag_quality_scorer.py "$note"
done < all_notes.txt > tag_quality_report.txt

# 3. 统计质量分布
grep "质量等级" tag_quality_report.txt | sort | uniq -c
```

### Dataview 查询: 低质量标签笔记

```dataview
TABLE
  file.name as "笔记",
  file.tags as "标签",
  length(file.tags) as "标签数量"
FROM ""
WHERE length(file.tags) < 3 OR length(file.tags) > 12
SORT file.mtime DESC
LIMIT 50
```

---

## 集成到工作流

### 1. 笔记创建时自动评分

在 Templater 模板中添加:

```javascript
<%*
// 创建笔记后自动评分
const scorer = require('5.Misc/Template/Scripts/tag_quality_scorer.py');
const score = scorer.score_note(tp.file.path);
if (score < 60) {
  tp.system.clipboard(`⚠️ 标签质量较低 (${score}/100), 建议改进`);
}
-%>
```

### 2. 定期质量检查

使用健康监控脚本 (`vault_health_monitor.py`) 自动检查:

```python
# 在 vault_health_monitor.py 中添加
def check_tag_quality(self):
    """检查标签质量"""
    low_quality_notes = []
    for note in self.all_notes:
        score, _ = self.scorer.score_note(note)
        if score < 60:
            low_quality_notes.append((note, score))
    return low_quality_notes
```

### 3. Git Hook 提交前检查

在 `.git/hooks/pre-commit` 中添加:

```bash
# 检查新增或修改的笔记的标签质量
for file in $(git diff --cached --name-only | grep "\.md$"); do
  score=$(python tag_quality_scorer.py "$file" | grep "标签质量评分" | awk '{print $2}')
  if [ "$score" -lt 40 ]; then
    echo "警告: $file 标签质量极差 ($score/100), 请改进后再提交"
    exit 1
  fi
done
```

---

## 相关资源

### 核心文档
- [Atlas/BASE/仓库标签管理系统.md](../../Atlas/BASE/仓库标签管理系统.md) - 标签规范
- [5.Misc/Template/Templater/tag_recommender.md](../../5.Misc/Template/Templater/tag_recommender.md) - 智能标签推荐
- [5.Misc/Template/Templater/tag_recommender_rules.md](../../5.Misc/Template/Templater/tag_recommender_rules.md) - 推荐规则库

### 相关工具
- Obsidian 插件: Tag Wrangler
- 脚本: tag_quality_scorer.py
- 模板: 5.Misc/Template/

---

**创建者**: Claude Sonnet 4.5
**最后更新**: 2026-01-26
**下次审查**: 2026-02-26
